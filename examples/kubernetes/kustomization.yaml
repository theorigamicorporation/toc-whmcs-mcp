apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
  # secret.example.yaml is a placeholder. Point this at a SealedSecret or an
  # ExternalSecret instead; do not commit real credentials.

commonLabels:
  app.kubernetes.io/name: toc-whmcs-mcp
  app.kubernetes.io/part-of: toc-platform
