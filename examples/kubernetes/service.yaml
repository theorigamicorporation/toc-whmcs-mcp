apiVersion: v1
kind: Service
metadata:
  name: toc-whmcs-mcp
  labels:
    app.kubernetes.io/name: toc-whmcs-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: toc-whmcs-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
